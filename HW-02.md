# Homework 02

## Bài 1

Cho 2 tập hợp $\{A, B, C, D, E\}$ và $\{1, 2, 3, 4\}$. Một chuỗi gồm 7 ký tự được tạo thành từ các phần tử của 2 tập hợp theo quy tắc, 4 phần tử liền kề nhau không được là ký tự số. Ví dụ: $A1234AC$ không hợp lệ và $123AB11$ là hợp lệ. Hỏi có bao nhiêu chuỗi hợp lệ?

- Tổng số chuỗi có thể tạo thành từ 2 tập hợp trên là: $9^7$ (vì có 9 phần tử trong 2 tập hợp).

- Các trường hợp tạo chuỗi không hợp lệ là:

    + 4 ký tự liền kề là số, có những dạng sau:

        * NNNNL__: có: $4^4.5.9^2$
        
        * LNNNNL_: có: $5.4^4.5.9$

        * _LNNNNL: có: $9.5.4^4.5$

        * __LNNNN: có: $9^2.5.4^4$

    + 5 ký tự liền kề là số, có những dạng sau:

        * NNNNNL_: có: $4^5.5.9$

        * LNNNNNL: có: $5.4^5.5$

        * _LNNNNN: có: $9.5.4^5$

    + 6 ký tự liền kề là số, có những dạng sau:

        * NNNNNNL: có: $4^6.5$

        * LNNNNNN: có: $5.4^6$

    + 7 ký tự liền kề là số, có dạng sau:
        * NNNNNNN: có: $4^7$

- Vậy số chuỗi hợp lệ là: $9^7 - (4^4.5.9^2 + 5.4^4.5.9 + 9.5.4^4.5 + 9^2.5.4^4 + 4^5.5.9 + 5.4^5.5 + 9.5.4^5 + 4^6.5 + 5.4^6 + 4^7)$.

## Bài 2

Cho a, b là 2 số lẻ thì a.b cũng là số lẻ

- Ta có: $a = 2m + 1$ và $b = 2n + 1$ với m, n là các số nguyên.

- Khi đó: $a.b = (2m + 1)(2n + 1) = 4mn + 2m + 2n + 1 = 2(2mn + m + n) + 1$.

- Vì $2mn + m + n$ là một số nguyên nên $a.b$ có dạng $2k + 1$ với k là một số nguyên, tức là $a.b$ là một số lẻ.

## Bài 3

Với x, y là số thực, x là số hữu tỉ và $xy-1$ là số vô tỉ thì y là số vô tỉ.

- Số hữu tỉ có dạng: $r = \frac{p}{q}$ với p, q là các số nguyên và $q \neq 0$.

- Ta chứng minh bài toán này bằng phương pháp phản chứng.

    + Giả sử $y$ là số hữu tỉ.

    + Ta có: $x$ là số hữu tỉ (giả thiết)

    + Nên $xy-1$ là số hữu tỉ, điều này mâu thuẫn với giả thiết $xy-1$ là số vô tỉ.

    + Tức là giả thiết $y$ là số hữu tỉ là sai, nên $y$ phải là số vô tỉ.


## Bài 4

Cho $f(x) = \dfrac{x+3}{x-2}$.

a) $f$ có phải là đơn ánh hay không?

- Giả sử: $f(x_1) = f(x_2)$ với $x_1, x_2 \in \mathbb{R} \setminus \{2\}$.
- Khi đó: $\dfrac{x_1+3}{x_1-2} = \dfrac{x_2+3}{x_2-2}$.
- Suy ra: $(x_1+3)(x_2-2) = (x_2+3)(x_1-2)$.
- Nhân phân phối vào từng vế: $x_1x_2 - 2x_1 + 3x_2 - 6 = x_1x_2 - 2x_2 + 3x_1 - 6$.
- Rút gọn: $-2x_1 + 3x_2 = -2x_2 + 3x_1$.
- Suy ra: $5x_1 = 5x_2$.
- Kết luận: $x_1 = x_2$.
- Vì $f(x_1) = f(x_2)$ dẫn đến $x_1 = x_2$, nên $f$ là đơn ánh.

b) f có phải là toàn ánh hay không?

- Ta cần chứng minh rằng với mọi $y \in \mathbb{R}$, tồn tại $x \in \mathbb{R} \setminus \{2\}$ sao cho $f(x) = y$.

- Giả sử $y \in \mathbb{R}$, ta cần tìm $x$ sao cho $\dfrac{x+3}{x-2} = y$.

- Suy ra: $x + 3 = y(x - 2)$.

- Rút gọn: $x + 3 = yx - 2y$.

- Suy ra: $x - yx = -2y - 3$.

- Rút gọn: $x(1 - y) = -2y - 3$.

- Nếu $y \neq 1$, ta có: $x = \dfrac{-2y - 3}{1 - y}$, và $x \neq 2$.

- Nếu $y = 1$, thì ta không tìm được $x$ sao cho $f(x) = 1$, vì $\dfrac{x+3}{x-2} = 1$ dẫn đến $x + 3 = x - 2$, điều này là vô lý.

- Kết luận: $f$ không phải là toàn ánh vì không tồn tại $x$ sao cho $f(x) = 1$.

## Câu 5

Cho $g(x,y) = (x+y, x-y)$.
a) $g$ có phải là đơn ánh hay không?

- Giả sử: $g(x_1, y_1) = g(x_2, y_2)$ với $(x_1, y_1), (x_2, y_2) \in \mathbb{R}^2$.

- Khi đó: $(x_1 + y_1, x_1 - y_1) = (x_2 + y_2, x_2 - y_2)$.

- Suy ra: $x_1 + y_1 = x_2 + y_2$ và $x_1 - y_1 = x_2 - y_2$.

- Cộng hai phương trình trên: $2x_1 = 2x_2$ dẫn đến $x_1 = x_2$.

- Thay $x_1 = x_2$ vào phương trình $x_1 + y_1 = x_2 + y_2$ ta có: $y_1 = y_2$.

- Kết luận: $g(x_1, y_1) = g(x_2, y_2)$, dẫn đến $(x_1, y_1) = (x_2, y_2)$, nên $g$ là đơn ánh.

b) g có phải là toàn ánh hay không?

- Ta cần chứng minh rằng với mọi $(a, b) \in \mathbb{R}^2$, tồn tại $(x, y) \in \mathbb{R}^2$ sao cho $g(x, y) = (a, b)$.

- Giả sử $(a, b) \in \mathbb{R}^2$, ta cần tìm $(x, y)$ sao cho $(x + y, x - y) = (a, b)$.

- Suy ra: $x + y = a$ và $x - y = b$.
- Cộng hai phương trình trên: $2x = a + b$ dẫn đến $x = \dfrac{a + b}{2}$.
- Thay $x$ vào phương trình $x + y = a$ ta có: $\dfrac{a + b}{2} + y = a$ dẫn đến $y = \dfrac{a - b}{2}$.
- Kết luận: Với mọi $(a, b) \in \mathbb{R}^2$, tồn tại $(x, y) = \left(\dfrac{a + b}{2}, \dfrac{a - b}{2}\right)$ sao cho $g(x, y) = (a, b)$, nên $g$ là toàn ánh.

## Câu 6

Cho $A = \{1, 2, 3, 4, 5\}$ và $2^A$ là tập lữu thừa của $A$. Với $f:2^A \to \{0, 1, 2, 3, 4, 5\} \times 2^A$ được định nghĩa bởi $f(B) = (|B|, A\setminus B)$.

a) Tính $f(\{1, 2, 3\})$.

- Ta có: $B = \{1, 2, 3\}$.

- Khi đó: $|B| = 3$ và $A\setminus B = \{4, 5\}$.

- Vậy: $f(\{1, 2, 3\}) = (3, \{4, 5\})$.

b) $f$ có phải là đơn ánh hay không?

- Giả sử: $f(B_1) = f(B_2)$

- Khi đó: $(|B_1|, A\setminus B_1) = (|B_2|, A\setminus B_2)$.

- Suy ra: $|B_1| = |B_2|$ và $A\setminus B_1 = A\setminus B_2$.

- Từ $A\setminus B_1 = A\setminus B_2$ suy ra: $B_1 = B_2$.

- Kết luận: $f(B_1) = f(B_2)$ dẫn đến $B_1 = B_2$, nên $f$ là đơn ánh.

c) $f$ có phải là toàn ánh hay không?

- Ta cần chứng minh rằng với mọi $(n, C) \in \{0, 1, 2, 3, 4, 5\} \times 2^A$, tồn tại $B \in 2^A$ sao cho $f(B) = (n, C)$.

- Giả sử $(n, C) \in \{0, 1, 2, 3, 4, 5\} \times 2^A$, ta cần tìm $B$ sao cho $f(B) = (n, C)$.

- Suy ra: $|B| = n$ và $A\setminus B = C$.

- Từ $A\setminus B = C$ suy ra: $B = A\setminus C$.

- Ta có: $n = |B| = |A\setminus C|$.

- Ví dụ: nếu chọn: $n=0$ và $C=\{1, 2\}$ thì $B = A\setminus C = \{3, 4, 5\}$ và $|B| = 3 \neq n$, điều này mâu thuẫn với giả thiết $|B| = n$.

- Vậy không tồn tại $B$ sao cho $f(B) = (n, C)$, nên $f$ không phải là toàn ánh.
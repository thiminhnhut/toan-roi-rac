# Homework 3

## Bài 1

### a) Chứng minh ~ là một quan hệ tương đương

Để chứng minh rằng ~ là một quan hệ tương đương, chúng ta cần chứng minh ba tính chất: phản xạ, đối xứng và bắc cầu.

- Tính phản xạ: \(\forall a \in A\), ta luôn có: \(f(a) = f(a)\). Do đó, a ~ a.

- Tính đối xứng: \(\forall a, b \in A\), nếu a ~ b, tức là \(f(a) = f(b)\), thì ta cũng có \(f(b) = f(a)\). Do đó, b ~ a.

- Tính bắc cầu: \(\forall a, b, c \in A\), nếu a ~ b và b ~ c, tức là \(f(a) = f(b)\) và \(f(b) = f(c)\), thì ta cũng có \(f(a) = f(c)\). Do đó, a ~ c.

### b) Mô tả các lớp tương đương

- Lớp tương đương của phần từ \(a \in A\): \([a] = \{x \in A | f(x) = f(a)\}\)

- Nếu xét theo \(n \in N\) thì lớp tương đương của \(n\) là tập nghịch ảnh \(f^{-1}(n)\), với điều kiện \(n\) phải thuộc tập giá trị của \(f\).


### c) Số lượng lớp tương đương tối đa

- Số lượng lớp tương đương tối đa là \(|A|\) (bằng với số phần tử của tập hợp \(A\)).


### d) Số lượng lớp tương đương tối thiểu

- Vì tập hợp \(A\) khác rỗng, nên số lượng lớp tương đương tối thiểu là 1.

## Bài 2

Giả sử \(m, n \geq 0\) và \(m + n \leq 10\). Có bao nhiêu dãy số gồm 10 chữ cái (cho phép các chữ cái được lặp lại), chứa chính xác \(m\) chữ cái 'A' và \(n\) chữ cái 'B'?

- Chọn \(m\) vị trí cho chữ 'A' trong 10 vị trí: \(\binom{10}{m}\) cách

- Chọn \(n\) vị trí cho chữ 'B' trong 10 vị trí còn lại: \(\binom{10 - m}{n}\) cách

- Ở các vị trí còn lại (10 - m - n) vị trí, ta có thể chọn bất kỳ chữ cái nào khác trong 24 chữ cái còn lại (không phải 'A' hoặc 'B'): \(24^{10 - m - n}\) cách

- Tổng số cách tạo ra dãy số là: \(\binom{10}{m} \cdot \binom{10 - m}{n} \cdot 24^{10 - m - n}\)

## Bài 3
Gieo 2 con súc sắc 8 mặt và có màu khác nhau.
### a) Ví dụ về 3 lần gieo khác nhau nhưng tương đương nhau

- Ví dụ ta xét tổng của 2 con súc sắc là 5, thì các lần gieo sau đây là tương đương nhau: \((1, 4), (2, 3), (3, 2)\).

### b) Chứng minh quan hệ tương đương

- Tính phản xạ: \(a + b = a + b\) nên (a, b) ~ (a, b).

- Tính đối xứng: \(a + b = a' + b'\) thì \(a' + b' = a + b\) nên (a', b') ~ (a, b).

- Tính bắc cầu: Nếu (a, b) ~ (a', b') và (a', b') ~ (a'', b''), tức là \(a + b = a' + b'\) và \(a' + b' = a'' + b''\), thì \(a + b = a'' + b''\) nên (a, b) ~ (a'', b'').

### c) Số lượng lớp tương đương

- Tổng của 2 con súc sắc có thể là từ 2 đến 16, nên có 16 - 2 + 1 = 15 lớp tương đương.

### d) Kích thước của lớp tương đương

- Kích thước tối thiểu là 1 (khi tổng là 2 hoặc 16, chỉ có một cách để đạt được tổng đó: (1, 1) và (8, 8)).

- Kích thước tối đa là 8 (khi tổng là 9, có 8 cách để đạt được tổng đó: (1, 8), (2, 7), (3, 6), (4, 5), (5, 4), (6, 3), (7, 2), (8, 1)).

## Bài 4

### a) Giải thích tại sao f ~ g khi và chỉ khi f và g nhận giá trị 1 trên cùng một số lượng đầu vào

- Vì hàm số \(f\) chỉ nhận giá trị 0 hoặc 1, nên \(\sum f(i)\) là đếm xem có bao nhiêu giá trị của \(f\) là 1, vì các số 0 không đóng góp vào tổng. Do đó, nếu \(f\) và \(g\) nhận giá trị 1 trên cùng một số lượng đầu vào, thì \(\sum f(i) = \sum g(i)\), và ngược lại.

### b) Chứng minh quan hệ tương đương

- Tính phản xạ: \(\sum f(i) = \sum f(i)\) nên f ~ f.

- Tính đối xứng: Nếu f ~ g, tức là \(\sum f(i) = \sum g(i)\), thì \(\sum g(i) = \sum f(i)\) nên g ~ f.

- Tính bắc cầu: Nếu f ~ g và g ~ h, tức là \(\sum f(i) = \sum g(i)\) và \(\sum g(i) = \sum h(i)\), thì \(\sum f(i) = \sum h(i)\) nên f ~ h.

### c) Các lớp tương đương khi n = 3

- Số lượng lớp tương đương là (n + 1). Với n = 3, có 4 lớp tương đương:
- Lớp 0: \(\{f | \sum f(i) = 0\}\): hàm số là \{(0, 0, 0)\}
- Lớp 1: \(\{f | \sum f(i) = 1\}\): hàm số là \{(1, 0, 0), (0, 1, 0), (0, 0, 1)\}
- Lớp 2: \(\{f | \sum f(i) = 2\}\): hàm số là \{(1, 1, 0), (1, 0, 1), (0, 1, 1)\}
- Lớp 3: \(\{f | \sum f(i) = 3\}\): hàm số là \{(1, 1, 1)\}

### d) Kích thước lớp tương đương tổng quát

- Để hàm số có đúng \(k\) giá trị là 1, ta chọn \(k\) vị trí trong \(n\) vị trí để đặt giá trị 1. Do đó, kích thước của lớp tương đương có tổng là \(k\) là \(\binom{n}{k}\).